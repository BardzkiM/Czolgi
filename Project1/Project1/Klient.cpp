#include "SFML\Network.hpp"

/*
sf::TcpSocket socket; // tworzymy gniazdo klienta
sf::IpAddress ip = sf::IpAddress::LocalHost;// "adres.ip.serwera.do.ktorego.sie.chcesz.polaczyc";
unsigned int port = 54000; // port na którym nas³uchuje serwer

if (socket.connect(ip, port) != sf::Socket::Done) // ³¹czymy siê z adresem 'ip' na porcie 'port'
// jeœli funkcja connect zwróci sf::Socket::Done oznacza to, ¿e wszystko posz³o dobrze
{
cerr << "Nie mo¿na po³¹czyæ siê z " << ip.toString() << endl;
exit(1);
}
else
{
	cerr << "Jestem klientem, w³aœnie nawi¹za³em po³¹czenie z serwerem";
	exit(1);
}
*/